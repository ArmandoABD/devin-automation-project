# ── Devin v3 API (service user) ─────────────────────────────────────────────
# Your service-user key (starts with cog_). Leave blank to run in DEMO_MODE.
DEVIN_API_KEY=
# Org ID from Settings > Service Users (required for v3 org-scoped routes).
DEVIN_ORG_ID=
# Override only if needed (defaults to the v3 host).
DEVIN_BASE_URL=https://api.devin.ai/v3

# ── GitHub (the Superset fork that receives issues + PRs) ────────────────────
GITHUB_TOKEN=
GITHUB_REPO=ArmandoABD/superset_armando

# ── Behavior ────────────────────────────────────────────────────────────────
# Force demo mode even if a key is present (true/false). Auto-on when no key.
DEMO_MODE=
# Optional: absolute path to a local Superset checkout for real before/after
# scan metrics (npm audit / pip-audit). Leave blank to skip local scanning.
REPO_PATH=
